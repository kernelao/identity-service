import { UniqueEntityId } from '@/domain/shared/UniqueEntityId';

/**
 * RefreshSessionId
 * ----------------
 * Identifie une session de refresh (en DB plus tard).
 */
export class RefreshSessionId extends UniqueEntityId {}
